import colors from './colors';
import typography from './typography';
import spacing, { layout } from './spacing';

export { colors, typography, spacing, layout };
export default { colors, typography, spacing, layout };
